
<!DOCTYPE html>
<html lang="en">
  <head>
    <% include ../partials/head %>
  </head>
  <body>

    <% include ../partials/header %>

    <main id="content" class="app-container" role="main">
      <div class="grid-row">
        <div class="column-two-thirds">
          <h1 class="heading-large">Add user</h1>

          <form class="form" action="<%= locals.constants.routes.ADD_USER %>" method="POST">
            <% if (success) { %>
              <div class="govuk-box-highlight u-ta-left">
                <h2 class="heading-medium u-no-margin-top">User was successfully added</h2>
                <p class="u-no-margin"><strong>Username:</strong> <%= newUser.username %></p>
                <p class="u-no-margin"><strong>Password:</strong> <%= newUser.password %></p>
              </div>
            <% }%>

            <% if (error) { %>
              <div class="error-summary" role="alert" aria-labelledby="error-summary-heading-2" tabindex="-1">
                <h2 class="heading-medium error-summary-heading u-no-margin-bottom" id="error-summary-heading-2">
                  Something went wrong! Please try again later.
                </h2>
              </div>
            <% }%>

            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <div class="form-group">
              <label class="form-label" for="accountType">Account Type</label>
              <select class="form-control" id="accountType" name="accountType" required>
                <option value="<%= locals.constants.USER_ACCOUNT %>">User account</option>
                <option value="<%= locals.constants.ADMIN_ACCOUNT %>">Admin account</option>
              </select>
            </div>
            <div class="form-group">
              <label class="form-label" for="username">Username</label>
              <input class="form-control" id="username" name="username" type="text" required />
            </div>
            <div class="form-group">
              <label class="form-label" for="password">Password</label>
              <input
                class="form-control"
                id="password"
                type="text"
                value="<%= randomPassword %>"
                disabled
              />
              <input type="hidden" name="password" value="<%= randomPassword %>">
            </div>
            <button class="button" type="submit">Add new user</button>
          </form>
        </div>
      </div>
    </main>

    <% include ../partials/footer %>
  </body>
</html>
